{
  "name": "enterprise-ai-runway-ui-checks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:access": "playwright test --config=playwright.access.config.js",
    "test:ui": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
